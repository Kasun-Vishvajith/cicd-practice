name: CI Pipeline
on:[push]

jobs:
  hello:
    runs-on:ubuntu-latest

    steps:
      - name: Say hello
        run: echo "Hello from Github Actions!"

      - name: show current directory
        run: pwd

      - name: list files
        run: ls -la
